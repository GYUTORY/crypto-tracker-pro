# Crypto Tracker Pro - 환경 변수 설정 예시
# ========================================
# 이 파일을 .env로 복사하고 실제 값으로 교체하세요
# 실제 API 키와 시크릿은 절대 Git에 커밋하지 마세요!

# ========================================
# 기본 설정
# ========================================
NODE_ENV=development
PORT=3000
HOST=0.0.0.0

# ========================================
# 바이낸스 API 설정
# ========================================
# 바이낸스 API 키 (실제 키로 교체 필요)
BINANCE_API_KEY=your-binance-api-key
BINANCE_SECRET_KEY=your-binance-secret-key
BINANCE_BASE_URL=https://api.binance.com
BINANCE_WS_URL=wss://stream.binance.com:9443/ws
BINANCE_TIMEOUT=5000
BINANCE_RETRY_ATTEMPTS=3

# ========================================
# 캐시 설정
# ========================================
CACHE_TTL=30000
CACHE_MAX_SIZE=1000
CACHE_CLEANUP_INTERVAL=60000

# ========================================
# 로깅 설정
# ========================================
LOG_LEVEL=info
LOG_FORMAT=json
LOG_FILE=logs/app.log

# ========================================
# 보안 설정
# ========================================
# JWT 시크릿 (실제로는 더 강력한 키 사용 권장)
JWT_SECRET=your-jwt-secret-key-min-32-characters-long-for-security
JWT_EXPIRES_IN=24h
BCRYPT_ROUNDS=10

# ========================================
# CORS 설정
# ========================================
CORS_ORIGIN=http://localhost:3000,http://localhost:3001,http://172.30.1.4:3000
CORS_CREDENTIALS=true

# ========================================
# Google AI 설정
# ========================================
# Google AI API 키 (실제 키로 교체 필요)
GOOGLE_AI_API_KEY=your-google-ai-api-key
GOOGLE_AI_MODEL=gemini-1.5-pro
GOOGLE_AI_TIMEOUT=10000

# ========================================
# Redis 설정 (선택사항)
# ========================================
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_DB=0

# ========================================
# 외부 API 설정 (선택사항)
# ========================================
EXCHANGE_RATE_API_KEY=your_exchange_rate_api_key_here
NEWS_API_KEY=your_news_api_key_here

# ========================================
# 개발 환경 전용 설정
# ========================================
# 개발 환경에서만 사용하는 설정들
# 프로덕션에서는 이 섹션을 제거하거나 주석 처리하세요

# 개발용 데이터베이스 (SQLite 사용 시)
# DB_HOST=localhost
# DB_PORT=5432
# DB_NAME=crypto_tracker_dev
# DB_USERNAME=postgres
# DB_PASSWORD=dev_password

# 개발용 로그 레벨 (더 상세한 로그)
# LOG_LEVEL=debug

# ========================================
# 프로덕션 환경 전용 설정
# ========================================
# 프로덕션 환경에서만 사용하는 설정들
# 개발 환경에서는 이 섹션을 주석 처리하세요

# 프로덕션용 강화된 보안 설정
# JWT_SECRET=your_production_super_secret_key_min_64_characters_long_for_maximum_security
# BCRYPT_ROUNDS=14

# 프로덕션용 로그 레벨
# LOG_LEVEL=warn

# 프로덕션용 CORS 설정 (실제 도메인으로 변경)
# CORS_ORIGIN=https://yourdomain.com,https://api.yourdomain.com

# ========================================
# 테스트 환경 전용 설정
# ========================================
# 테스트 환경에서만 사용하는 설정들
# NODE_ENV=test
# DB_NAME=crypto_tracker_test
# LOG_LEVEL=error
